#include <bits/stdc++.h>
#define FOR(a, b) for(int i = a ; i < b; i++)
#define FORI(i, a, b) for(i = a; i < b ; i++)
#define all(x) x.begin(), x.end()
using namespace std;
void solve(){
    int n, k;
    cin >> n >> k;
    vector<int> X(n), Y(n);
    FOR(0, n) {
        cin >> X[i];
    }
    FOR(0, n) {
        cin >> Y[i];
    }
    sort(all(X));
    int j = n - 1;
    vector<int> L(n), R(n);
    for(int i = n - 1 ; i >= 0 ; i--) {
        while
    }
}
int main() {
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}